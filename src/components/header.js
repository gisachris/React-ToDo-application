import React from 'react';

const Header = () => (
  <section className="headingArea">
    <h1 className="topHeading">todos</h1>
    <p className="topParagraph"> items will persist in the local Storage</p>
  </section>
);

export default Header;
