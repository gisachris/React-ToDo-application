import React from 'react';

const InputField = () => (
  <section className="inputSection">
    <input type="text" required placeholder="enter some task to accomplish" />
  </section>
);

export default InputField;
